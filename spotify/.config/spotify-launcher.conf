# O processo de GPU do Spotify morre com "GPU process isn't usable. Goodbye." na RX 550:
# o backend padrao do ANGLE nao sobe nesta Polaris. Forcar o backend GL mantem aceleracao
# e sandbox, ao contrario de --disable-gpu.
[spotify]
extra_arguments = ["--use-gl=angle", "--use-angle=gl"]
