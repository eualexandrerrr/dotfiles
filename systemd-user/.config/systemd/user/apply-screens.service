[Unit]
Description=Reforca a config de tela (taxa, rotacao, posicao)
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=oneshot
ExecStart=%h/.dotfiles/bin/apply-screens.sh

[Install]
WantedBy=graphical-session.target
